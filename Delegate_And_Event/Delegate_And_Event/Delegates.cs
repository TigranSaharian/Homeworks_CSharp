namespace Delegate_And_Event
{
    class Delegates
    {
        // The delegate for the StringHalper class
        public delegate int CountDelegate(string message);

        // The delegate for the student class
        public delegate void ShowMessage(string message);

    }
}
